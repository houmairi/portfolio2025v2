---
const { logoSrc, label } = Astro.props;
---

<div class="px-3 py-2 rounded-md border border-neutral-300 hover:border-neutral-800 hover:shadow-sm transition-colors inline-flex items-center gap-2 bg-white">
  <img src={logoSrc} alt={label + ' logo'} class="w-6 h-6 rounded" />
  <span class="text-neutral-500 hover:text-neutral-800 text-base font-normal">{label}</span>
</div>
